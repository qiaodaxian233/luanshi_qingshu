# 乔大仙 · 全能助手 项目对话记忆

## 项目概述

- **仓库**: https://github.com/qiaodaxian233/luanshi_qingshu (public)
- **作者**: qiaodaxian233 (乱世情书 Project)
- **类型**: 油猴 / Tampermonkey 用户脚本
- **运行环境**: ChatGPT (chatgpt.com / chat.openai.com / gpt.aimonkey.plus)
- **核心定位**: ChatGPT 网页端的"瑞士军刀"工具,主打**漫画分镜批量生成 + 视频生成上游素材准备**

## 用户的真实工作流(关键背景)

整个工具是为下面这条端到端流水线服务的:

```
[小说/剧本 TXT]
   ↓
[ChatGPT + GPT-image-2]   ← 本脚本在这里发挥作用
   ↓ 生成漫画风分镜帧
[本地下载并按 NN_前缀_序号.png 命名]   ← 本脚本「图片」tab
   ↓
[ComfyUI + Wan 2.2 + AutoSequentialFrames 节点]   ← 另一个仓库
   ↓ 首尾帧模式批量生成视频段
[剪辑拼接成完整动画]
```

**关联仓库**: `qiaodaxian233/AutoSequentialFrames` 是这条流水线的下游。两个项目互相独立但配套使用。

## 工具的四大模块(4 个 tab)

### ✍ 提示词 tab
- 内置 30+ 提示词,分类:image / write / code / translate / analyze / role / custom
- 支持收藏 ⭐、自定义保存、搜索高亮
- 写入模式:替换 / 追加 / 前置
- **重要内置**:
  - `img_09` GPT-image2 暗黑硬派漫画(单图)— **用户主要用的风格**
  - `img_10` GPT-image2 分镜九宫格漫画页

### 🎵 图片 tab
- **批量下载 GPT 生图**:自动识别 GPT 域名(`oaidalleapiprodscus.blob.core.windows.net` / `files.oaiusercontent.com` 等),自动监听新生成的图,按 `NN_前缀_序号.png` 命名(如 `01_最强异能_1.png`),序号自动递增
- **批量上传图片**:拖拽文件夹 / 选图片 → 串行上传 → 等 GPT 生图完成 → 自动推进下一张
- **三重兜底**:`input.files` → 点击附加按钮 → `ClipboardEvent paste`,扛 ChatGPT DOM 频繁改动

### 🎭 角色库 tab
- 多张角色参考图持久化(base64 dataURL 存 GM_setValue)
- 支持多选,选中带金边和 ① ② ③ 编号
- "上传选中"一次性把所有选中的角色图附加到 ChatGPT 输入框
- 文件名识别:`张三.png` / `01_张三_背面.png` 都能被项目 tab 模糊匹配到

### 📋 项目 tab(本工具的"心脏")
v6.0+ 引入,把上面三个模块串起来:
1. 上传 TXT 剧本
2. 自动从 TXT 提取人名(姓氏滑窗 + 引号 + 对话标签 + 动词锚定 + 英文人名)
3. 模糊匹配到角色库的图(文件名包含人名即匹配)
4. 选风格(内置 / 自定义)
5. 写额外提示词
6. 一键应用 = 角色图 + TXT 附件 + 提示词全部就绪,用户检查后手动发送

## 版本演进史

### v5.x — 早期版本
- 三个模块独立(提示词 / 图片 / 角色库)
- `STORE_KEY = 'luanshi_combined_v5'` 沿用至今,平滑迁移

### v6.0 — 项目模块诞生
- 新增「📋 项目」tab
- TXT 剧本 → 人名提取 → 角色库匹配 → 一键应用

### v6.0.1 — 项目风格自定义
- 项目 tab 风格提示词支持「✏ 编辑 / ＋ 新建」用户自定义风格
- 内置风格不可改,编辑时自动转「另存为新风格」

### v6.0.2 — TXT 附件
- 应用项目时把 TXT 剧本作为附件一起传给 GPT
- 文件名带 BOM 防中文乱码
- 改 `findFileInput` 优先选通用 input(可同时收图+文档)

### v6.1.0 — 配置导入/导出
- 完整导出(含角色图 base64,几 MB~几十 MB)
- 轻量导出(无角色图,几 KB,适合分享配置)
- 导入用合并策略(`mergeArrayById` / `mergeArrayUnique`)而非覆盖
- 一键重置(双重确认,清空所有数据)

### v6.2.0 ⭐ 当前版本(2026-05-02)— 剧本自动分段

**根因**:整本小说/整章剧本太长(第一章 5234 字),直接发给 GPT 会:
- 超出输入框限制
- GPT 处理质量下降
- 一次生成太多分镜导致人物一致性丢失

**v6.2.0 解决方案**:

- **新切分算法** `splitScript`
  - 优先级:章节标题 > 段落空行 > 句子边界
  - 目标字数 1000(可调 300-3000)
  - 浮动范围 ±50%(MIN=500 / MAX=1600)
  - 单段超过 MAX 时段内强切(`subdivide` 按句号/问号/感叹号)

- **前情提要** `extractRecap`
  - 从上一段末尾抽取约 80 字
  - 必须从句号/换行处开始,保证完整句子
  - GPT 端用 `【前情提要(已绘制,无需重画)】` 标记区分

- **场景识别** `detectScene`
  - 识别"中天大厦"、"一间出租屋里"等地点描述
  - 在段列表用 📍 标签显示

- **新 UI section**「📖 自动分段」
  - 目标字数输入框
  - 🔄 重新分段按钮
  - 段列表(金边高亮当前段,字数过短蓝色/过长红色警告)
  - ◀ 上一段 / 下一段 ▶ / 📋 复制本段

- **applyProject 改造**
  - 有分段就只发当前段,没分段就发整篇
  - 文件名带 `_第NN段_共MM段` 后缀
  - 提示用户:`✓ 第 X/Y 段已就绪`

- **配置导入/导出**支持新字段:
  - `projectTargetLen` / `projectOverlapChars` / `projectShowSegPanel`
  - 衍生数据(`projectSegments` / `projectCurrentSegIdx`)不导出,导入剧本后自动重算

- **实测**(《最强异能学生》第一章 5234 字):
  - 自动分 5 段
  - 字数:1115 / 1029 / 1014 / 1009 / 620
  - 前情提要:106 / 113 / 106 / 110 字
  - **关键边界**:林渊死亡 → 林默醒来,刚好落在第 2/3 段交界 ✓

**向后兼容**:
- 没分段时所有逻辑等同 v6.1.0
- DEFAULT 字段补齐保证旧用户平滑升级
- `STORE_KEY` 不变

## 文件结构

```
luanshi_qingshu/
├── README.md
├── 🎭 乔大仙 · 全能助手(项目联动版)-6.2.0.user.js   # 主脚本, 3532 行
└── 项目对接.md                                       # 本文件
```

**注意**:文件名带版本号(`-6.2.0.user.js`),每次升级会 `git mv` 重命名。脚本头部**没有 `@updateURL`**,所以重命名不影响用户已安装版本(Tampermonkey 按 @namespace + @name 识别)。

## 已知问题与未来改进

### v6.2.0 已知小问题
- **场景识别覆盖率低**:第一章测试中 5 段都没识别到 📍(原文里独立场景行少,场景切换都内嵌在动作描述里)。不影响分段,只是 UI 上少了个标签。
- **姓氏滑窗误伤**:`马 / 向 / 习 / 解` 等高频字误识别为姓("马上"、"向前"等)。已知,未修。

### 之前指出但用户暂未采纳的建议
- 角色库 base64 → IndexedDB(扛 100+ 张图)
- splitScript / fuzzyMatchRoles 加单元测试
- ProseMirror-safe 的提示词写入(替代 deprecated 的 `execCommand`)
- 拆模块(`@require` 拆 5 个文件)
- `runUploadQueue` 并发锁前置(防快速双击)

### 可加的下一波功能
- **手动调整切点**:段列表上加"在此分裂 / 与下段合并"按钮
- **形象标签**:每段加 `figureTag` 字段(如"林渊形象"/"林默形象"),自动匹配对应角色图
- **自动连发**:监听「图片已创建」N 张后自动推进下一段
- **段间统计**:显示每段实际生图数量,反向调优目标字数

## 协作模式(推代码流程)

**与 AutoSequentialFrames 仓库保持一致**:

1. 用户喜欢 GitHub 网页直接更新,不喜欢手动下载 zip(但脚本无 @updateURL,只能手动重装)
2. **PAT 流程**:
   - 用户创建一次性 **fine-grained PAT**
   - 范围:Only `luanshi_qingshu`,Permissions Contents: Read and write
   - Claude 用 `https://x-access-token:${PAT}@github.com/...` 一次性塞进 URL,**不写进 git config**
   - 推完(包括 tag)后用户立即去 https://github.com/settings/tokens 撤销 PAT
   - **验证撤销**:用废 PAT 尝试**写操作**(推一个临时 tag),返回 `Invalid username or token` 才算真撤销
   - 不能用 `git ls-remote` 验证(public 仓库读不需要 auth)
3. 日志输出用 `sed "s/${PAT}/<REDACTED>/g"` 擦掉

## 用户工作风格

- **提需求方式**:倾向一次列多个问题,期望 Claude 先复述理解 + 用 `ask_user_input_v0` 对齐关键决策点再动手
- **风格偏好**:简洁直接,不喜欢冗余客套
- **决策风格**:看到方案后秒拍板("直接 commit 到 main"/"打 tag"/"整文件替换"),不纠结细节
- **关键警觉点**:对**人物形象一致性**极敏感(因为是给 Wan 2.2 视频生成准备素材,人物漂了整段视频报废)
- **不喜欢的事**:
  - 让他手动改代码(怕改错,所以宁愿 Claude 整个推送)
  - 重复纠结已经决定的事
  - 假设他懂某些行业术语
- **偏好的协作节奏**:Claude 把活全干完,用户最后审查 + 拍板

## 推送记录

### v6.2.0(2026-05-02)
- commit: `c30d7e8` (1 file changed, +463 / -51, 88% rename similarity)
- 推到 master: `22b611a..c30d7e8`
- tag: `v6.2.0` (annotated, 含完整 release notes)
- 文件重命名:`-6.1.0.user.js` → `-6.2.0.user.js`
- PAT 用完已撤销并验证

### 推送前验证清单
- ✅ `node --check` 语法检查通过
- ✅ 29 项关键标记存在性检查全过(版本号 / 字段 / 函数 / UI 元素 / CSS 类 / 配置导入导出)
- ✅ 用《最强异能学生》第一章实测分段(5 段,字数贴 target,前情提要正确)

## 调试速查

### 用户报"分段不对"
- 先看「📋 复制本段」出来的内容,检查【前情提要】和【本段正文】
- 调整目标字数(默认 1000,可试 800/1200)再点「🔄 重新分段」
- 关键边界落不准 → 短期手动调,长期等 v6.3.0 加手动切点功能

### 用户报"角色图附加失败"
- 让他先手动点一下 ChatGPT 输入框,再点「🚀 应用」(三重兜底要 contenteditable focus)
- F12 看 console 是否有 `[AutoSeq Ctrl]` 类似日志(角色库 / 上传是否能找到 input[type=file])

### 用户报"配置导入失败"
- 检查文件 `_meta.app === '乔大仙·全能助手'`
- 大文件(含角色图)可能超过 GM_setValue 上限(各 Tampermonkey fork 不一样,Violentmonkey 严格)
- 提示用户用「轻量导出」(不含角色图)

### 用户报"重启后角色库丢了"
- 几乎可以肯定是 GM_setValue 容量爆了
- v6.1.0 的 catch 会打 console error,但用户感觉就是"图没了"
- 临时方案:让用户先「📤 完整导出」备份,然后清掉一些不常用的角色图
- 长期方案:迁移到 IndexedDB(已建议,未做)

## 安全提醒

- 仓库是 public,任何人都能读
- 不要把 PAT 写进任何 commit / 日志 / 文档
- 用完立刻撤销 PAT
- 验证撤销时**必须用写操作**(推 tag),`git ls-remote` 在 public 仓库永远成功,不能反映 token 状态

---

**对接完毕**。新对话中 Claude 看到此文档,可直接基于以上信息继续协助处理后续版本迭代、bug 修复、或新功能开发。
